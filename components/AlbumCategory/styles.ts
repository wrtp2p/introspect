import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    margin: 10,
    paddingTop: 5,
  },
  title: {
    color: 'black',
    fontSize: 30,
    fontWeight: 'bold',
    margin: 10
  }
});

export default styles;
